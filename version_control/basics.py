# central version control store everything in a central server
# distributed version control store everything in a local repository
# git is a distributed version control system

#if the central get fails , then central control system fails
# but in distributed version control system , even if the central server fails
# the local repository will be there and we can push it to another central server

